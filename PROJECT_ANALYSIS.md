# PROJECT_ANALYSIS.md

**対象**: CONTOUR — 市街戦 (`lunex773-lab/ge-mu`, branch `claude/multiplayer-login-c9n7dk`)
**目的**: BOSS NEURAL CORE 仕様書 (v1.0) の STEP 1–4 に基づく既存プロジェクト調査
**日付**: 2026-09-22 / 分析時点の HEAD: `05a4271`

> 本書は仕様書 §39 STEP 2 の成果物です。仕様書の指示どおり、**この分析が承認されるまで大規模なコード変更は行いません。**

---

## 1. 現在のアーキテクチャ

仕様書 §1 が要求する項目に対して、実測した回答です。

| 項目 | 実際 |
|---|---|
| ゲームエンジン | **なし**。素の WebGL + Three.js **r128**（CDN） |
| 言語 | ブラウザ JavaScript（ES2020相当）、**トランスパイルなし** |
| フレームワーク | なし。npm なし、**ビルドステップなし** |
| ディレクトリ構造 | **存在しない**。リポジトリは `index.html` と `README.md` の2ファイルのみ |
| ファイル規模 | `index.html` = **11,114行**（HTML + CSS + 単一 `<script>`） |
| モジュール境界 | 698行目の `(() => { 'use strict';` から 11,111行目の `})();` まで**全体が1つのIIFE**。外部スコープへ露出する識別子は**ゼロ** |
| 外部依存 | `three.min.js` (cdnjs, r128)、`mqtt.min.js` (jsDelivr, v5) の2つのみ |
| Networking | **サーバーレス**。公開MQTTブローカー経由のP2P風。ホスト選出でモブAIの実行権を決定 |
| Game Loop | `frame(now)` (10,956行) → `requestAnimationFrame`。**dt は 0.05 秒に clamp** |
| State Management | フレームワークなし。モジュールスコープの変数群 + プレーンオブジェクトの配列 |
| Physics | 自前。AABB押し出し（`cityCollide` / `wreckCollide`）+ サブステップ |
| Animation | 自前。共有スケルトンを1体ずつポーズして `InstancedMesh` に書き込む方式 |
| 設定ファイル | **なし**。設定は `localStorage`（`contour.*` キー）に直接保存 |
| 既存テスト | **リポジトリ内には存在しない**。Playwright スクリプト群はスクラッチパッドにのみ存在し、コミットされていない |
| ログシステム | **なし**。F3 デバッグオーバーレイが唯一の可視化 |
| イベントシステム | **なし**。関数直接呼び出し |

### 重要な構造的特徴

- **単一ファイル・単一スコープ**。仕様書 §4 が想定するディレクトリ構造（`connectome/`, `neural/`, `memory/` …）は、この形のままでは物理的に作れません。→ **質問 Q1**
- **IIFE の外から中は一切見えません**。テスト用に `window.__t` フックをビルド時に注入する仕組みをスクラッチパッドに持っていますが、本体にはありません。
- **描画は人口と分離**。1種族あたりドローコール5固定（詰めたスロットへ書き込む方式）。

---

## 2. AI関連コード（既存）

現状のモブAIは4種類で、**すでに仕様書の一部を独自に満たしています**。

| モブ | 判断関数 | 方式 | 行 |
|---|---|---|---|
| デモドッグ | `dogDecide` / `dogTick` | **効用スコアリング**（FSMではない） | 5473 / 5537 |
| デモドッグ（群れ） | `dogPackTick` | 役割配分・知識伝播 | 5762 |
| デモゴルゴン | `gorThink` | FSM（switch） | 6677 |
| マインドフレイヤー | `mfThink` | FSM + 指揮ネットワーク | 8002 |
| VECNA | `vecThink` | FSM + フェーズ + 指揮 | 9468 |

### 既存の「Tactical Brain 相当」

`dogDecide` は仕様書 §16 の utility_score 方式を**すでに実装しています**:

```js
S.chase = known ? 0.55 + (allies - 1) * 0.13 + hpF * 0.35
        - Math.max(0, dist - D_LIKED) * 0.012 - (engaged >= 2 ? 0.55 : 0) : 0;
```

距離・味方数・自分のHP・状態継続ボーナス・役割修正を足し引きして最大値を選ぶ構造で、**仕様書 §16 の式とほぼ同型**です。これは再利用対象です。

### 既存の「Player Model 相当」

VECNA に**プレイヤー速度の移動平均**がすでにあります（9,491行）:

```js
const vxp = (p.x - vec.lastPX) / Math.max(1e-3, dt);
vec.dodgeX += (vxp - vec.dodgeX) * Math.min(1, dt * 1.4);
```

仕様書 §13–15 の Player Behavior Model の**最小の種**がここにあります。ただし「速度の平滑化」止まりで、行動分類・頻度・遷移確率・予測は**ありません**。

### 既存の Sensor Layer 相当

| 感覚 | 関数 | 内容 |
|---|---|---|
| 視覚 | `dogSee` / `gorSee` / `mfSee` / `vecSee` | 距離 + 視野角 + **レイキャストによる遮蔽判定** + cloak補正 |
| 聴覚 | `dogHear` | 音イベントリング（銃声・足音・咆哮）に減衰付き |

仕様書 §11 の要求（position / velocity / health / visibility / distance / cover）に対し、**視覚・聴覚・距離・遮蔽はすでに実装済み**。プレイヤーの attack/dodge/skill 状態は**取得していません**。

### 既存の Fairness 相当（§17）

仕様書 §17 が要求する制約の多くが**すでに設計に入っています**:

- 反応遅延: `atkCd`、状態ごとの `dwell` 時間
- 不完全情報: レイキャスト遮蔽、視野角、`cloak` による発見率低下
- AI LOD: 遠方個体は 1/24 フレームでしか思考しない
- **プレイヤーの入力（キー入力）は一切参照していません** — 仕様書 §17 の禁止事項を既存コードは守っています

---

## 3. 変更してはいけない部分

| 部分 | 理由 |
|---|---|
| `frame()` のホットパス（10,956–11,110） | 60FPS予算。ここに重い計算を足すと携帯で破綻 |
| `InstancedMesh` 書き込み（`writeDog` / `writeGor` / `writeFlayer`） | ドローコール5固定の要。スロット詰め順に依存 |
| リグ構築とインスタンス予約数（`M_OVOID` 等） | ガードが厳密一致を要求。ずれると `console.error` |
| MQTT スナップショット形式（`publishMobs` / `applyDogSnapshot` 等） | 既存クライアントとの互換。**帯域が直接コストになる** |
| `mobSim()` / `mobPub()` のオーナー選出 | ここを壊すとマルチで2重にAIが走る |
| `easeHeading`、`dogStep` / `gorStep` のサブステップ | 直近で2つの実バグを直した箇所。触ると回帰する |
| `dogDecide` の役割加点 `if (known)` ガード | 「マップ中心に集まる」不具合の修正本体 |

## 4. 再利用可能な部分

| 部分 | Neural Core 側での用途 |
|---|---|
| `dogSee` / `gorSee` / `mfSee` / `vecSee` | **Sensor Layer をそのまま流用**（§11） |
| `dogHear` / `dogNoises` | 聴覚センサー（§11） |
| `dogDecide` の効用スコア構造 | **Tactical Brain の骨格**（§16）。スコア源を1つ足すだけで接続可能 |
| `vec.dodgeX/dodgeZ` | Player Model の速度推定（§13） |
| `rayCity` / `rayWrecks` | 遮蔽・cover 判定（§11） |
| LOD の `every` ストライド | **更新周波数分離（§28）がすでにこの形**。Neural Core も同じ仕組みに乗せられる |
| F3 オーバーレイ (`dbgOn`, `vecDebug`) | Debug UI（§30）の下地 |
| `window.__t` テストハーネス（スクラッチパッド） | Unit/Integration Test の足場（§33） |

## 5. Neural Core 接続ポイント

**最小侵襲の接続点は1箇所だけです。**

```
既存:  vecThink(dt) → switch (vec.st) { … }            // FSM が行動を決める
接続後: vecThink(dt) → NeuralBossAdapter.decide(ws)     // スコアを返す
                     → 既存の switch / vecStartAtk() へ  // 実行は既存のまま
```

具体的な差し込み位置の候補:

| 候補 | 場所 | 侵襲度 | 備考 |
|---|---|---|---|
| **A. 新規モブ** | 新しい `xxxThink()` を追加 | **最小**。既存AIに一切触れない | 仕様書 §32 Fallback も自然（AI停止→既存FSMで動く別モブ） |
| B. VECNA を置換 | `vecThink` の switch 前 | 中。既存のフェーズ・演出と競合しうる | 既存のボス体験を壊すリスク |
| C. デモゴルゴン | `gorThink` | 中 | 6体いるので学習対象として多すぎる |

→ ユーザーの指示は「**敵モブを作成**」なので **A** が素直ですが、確認が必要です → **質問 Q2**

**WorldState の境界**（§12）は、既存の `p` / `vec` / `dogs` を Neural Core が直接見ないよう、**1つのプレーンオブジェクトに詰め替えて渡す**形にします。これは仕様書 §12 の要求そのものです。

## 6. 技術的リスク

| # | リスク | 深刻度 | 対策案 |
|---|---|---|---|
| R1 | **仕様書の想定構造とプロジェクトの形が根本的に違う**。`boss-neural-core/` のような多階層ディレクトリ + Python風RLパイプラインは、ビルドなし単一HTMLに乗らない | **高** | → **質問 Q1** |
| R2 | **携帯での性能**。既にデモドッグ48体 + ゴルゴン + フレイヤー + VECNA が動いている。1000–5000ノードの行列演算を毎フレームは不可能 | **高** | 更新周波数分離（§28）+ ノード数を実測で決める（§8は固定値にするなと明記） |
| R3 | **FlyWire実データ**。ライセンス確認・登録が必要な可能性、サイズが大きい、`data/` の .gitignore 必須（§27） | 中 | → **質問 Q3** |
| R4 | **マルチプレイ**。AIはオーナー端末でしか走らない。学習した重みを他端末へ送るのか、各自が別々に学習するのか未定義 | 中 | → **質問 Q5** |
| R5 | **単一ファイルのサイズ**。現在11,114行。Neural Core を同居させると可読性と編集リスクが上がる | 中 | → **質問 Q1** |
| R6 | **ロールバック事故**。本セッション中にコンテナのロールバックが3回発生し、作業ツリーが巻き戻った | 中 | 各Phase終了ごとに必ずコミット・プッシュする |
| R7 | **学習が「不公平」に振れる**（§17, §40） | 中 | FairnessController を Phase 4 で先に入れ、後付けにしない |
| R8 | `dt` が 0.05 に clamp されている | 低 | Neural Core の時間積分は実時間ではなくゲーム内 `cityT` を使う |

## 7. 推奨実装順序

仕様書 §35 のフェーズを、**このプロジェクトの制約に合わせて並べ替えた案**です。

| Phase | 内容 | 成果物 | 検証 |
|---|---|---|---|
| **0** | 本書 + 承認 | `PROJECT_ANALYSIS.md` | ― |
| **1** | Mock Connectome + グラフ生成 + 圧縮 | 決定論的な擬似コネクトーム生成器 | ノード数別の生成テスト |
| **2** | Neural Core（活性・抑制・減衰・再帰） | `WorldState → ActionScores` | 単体テスト、NaN耐性 |
| **3** | Sensor Layer / WorldState 境界 | 既存 `*See` を詰め替える層 | 既存AIに影響がないこと |
| **4** | **FairnessController を先に** | 反応遅延・確信度閾値・記憶長 | 難易度別の実測 |
| **5** | Player Model（行動分類 + n-gram + 予測） | 予測確率 + confidence | 予測精度の測定 |
| **6** | Tactical Brain + Action Selector | 効用統合（既存 `dogDecide` 型） | Ablation A/B/C/D |
| **7** | 新規モブの身体・アニメ・音 | 見た目と当たり判定 | スクリーンショット検証 |
| **8** | Debug UI + Replay | F3 拡張、判断ログ | §30 の可視化 |
| **9** | Boss AI Lab（ヘッドレス戦闘シム） | Player Bot 6種 + 評価指標 | 勝率・生存時間 |
| **10** | 強化学習（オフライン） | 重みエクスポート | Ablation E |
| **11** | FlyWire実データ導入（任意） | 実データ→圧縮→比較 | §25 Ablation |
| **12** | 最適化・本編統合 | ― | FPS影響測定 |

**Fairness を §17 の位置（後半）ではなく Phase 4 に前倒し**しています。理由: 後付けの公平性制約は、すでに強くなったAIを弱める作業になり、調整が難航するためです。

## 8. 計算負荷 — 推定と、実測による訂正

**最初に書いた見積もりは間違っていました。** Phase 1 の実測で訂正します。

当初の推定（実測前の机上計算）:

| ノード数 | 判定（推定） |
|---|---|
| 1000 | 要実測 |
| 2500 | 携帯では厳しい可能性 |
| 5000 | **携帯では非現実的** |

**実測**（ワークステーション、1推論スイープ＝全エッジを1回舐める。Neural Core の下限コスト）:

| ノード数 | エッジ数 | ms/tick | 1フレーム(16.7ms)に占める割合 | メモリ(CSR) |
|---|---|---|---|---|
| 100 | 1,223 | 0.021 | 0.1% | 10K |
| 500 | 6,147 | 0.042 | 0.3% | 50K |
| 1000 | 12,270 | 0.050 | 0.3% | 100K |
| 2500 | 30,677 | 0.124 | 0.7% | 249K |
| 5000 | 61,418 | 0.274 | **1.6%** | 499K |

5000ノードでも1フレームの **1.6%** です。携帯が5〜10倍遅いと仮定しても 8〜16%、しかも §28 の周波数分離で 10Hz なら6フレームに1回しか走りません。**「5000は非現実的」は誤りでした。**

ただしこれは**下限**です。Phase 2 の実際の dynamics（減衰・再帰・状態保持）と、Phase 5 以降の Player Model が乗ります。最終判断は Phase 2 で**実機実測**してから行います（D8「FPSを一切下げない」）。

### 圧縮戦略の比較（§8）— これが本題でした

5000 → 500:

| 戦略 | ノード | エッジ | 平均次数 | **到達性** | 孤立 | ms |
|---|---|---|---|---|---|---|
| degree | 500 | 3,572 | 14.29 | 1 | 2 | 7 |
| region | 500 | 3,495 | 13.98 | 1 | 3 | 9 |
| community | 500 | 2,435 | 9.74 | **0** | 0 | 181 |
| path | 500 | 3,318 | 13.27 | 1 | 0 | 6 |
| hybrid | 500 | 2,323 | 9.29 | 1 | 0 | 7 |

5000 → 150（**厳しい圧縮**。携帯で実際に走る領域）:

| 戦略 | ノード | エッジ | 平均次数 | **到達性** | 孤立 | ms |
|---|---|---|---|---|---|---|
| degree | 150 | 807 | 10.76 | **0** | 1 | 2 |
| region | 150 | 807 | 10.76 | **0** | 1 | 2 |
| community | 150 | 153 | 2.04 | **0** | 25 | 84 |
| **path** | 150 | 521 | 6.95 | **1** | 6 | 2 |
| **hybrid** | 150 | 365 | 4.87 | **1** | 9 | 3 |

**到達性 = 感覚ニューロンから信号が届く運動ニューロンの割合。**

緩い圧縮では4戦略が横並びで、比較は何も語りません。**厳しく圧縮して初めて差が出ます**: 次数・領域・コミュニティの3つは、感覚から運動への経路を**完全に切断**します（到達性 0）。小さくはなりましたが、脳ではなく瓦礫です。

`path` と `hybrid` だけが導通を保ちます。**デフォルトは `hybrid`** とします。

> コミュニティ戦略が 500 でも 0 なのはバグではなく設計上の帰結です。最大コミュニティを丸ごと残す方式は局所回路（＝再帰性の在処）を保ちますが、この mock ではコミュニティが領域内ブロックなので、感覚→運動を跨ぎません。「局所回路は残るが経路は切れる」というトレードオフがそのまま数字に出ています。

---
## 9. 確定した決定事項

本書作成中に出た判断は、すべてユーザーに確認して確定させました（D1〜D8 は初回、D9〜D12 は参考画像の受領後）。

| # | 論点 | **決定** | 帰結 |
|---|---|---|---|
| D1 | 成果物の形 | **同居＋別ラボ** | Neural Core は `index.html` 内の区画。学習・シミュレーションは `lab/` で出荷しない |
| D2 | 対象モブ | **完全新規** | 既存AIに一切触れない。§32 Fallback が自然に成立 |
| D3 | データ | **Mockのみで全工程** | ライセンス・容量・ネットワークの問題が発生しない。実データは後から差し替え可能な境界を保つ |
| D4 | 学習の永続性 | **localStorage で永続** | §40「俺の戦い方を覚えている」を優先。§17 Fairness で攻略不能化を防ぐ |
| D5 | 位置づけ | **単独の狩人・1体限定** | 「同じ個体が覚えている」が体験の核心。性能予算も最小 |
| D6 | 見た目 | **後で参考画像** | Phase 7 までは仮の形（デモゴルゴンの骨格流用）で動かす |
| D7 | マルチ | **オーナーが学習、記憶は各自** | 帯域増加ゼロ。プロファイルは同期しない |
| D8 | 性能予算 | **FPSを一切下げない** | 実測で入るノード数を選ぶ。§28 周波数分離を徹底 |
| D9 | 名前と姿 | **ヴェルゼブブ**（参考画像 `lab/ref/beelzebub.jpg`） | 人型・重装甲・骨の大鎌・昆虫翅4枚。D6 の「後で」はこれで解消 |
| D10 | 出現 | **ゲートの守護神。表世界。ゲート出現と同時にゲート前へ** | ルーム全員が同じゲートを見ることが前提 → ゲート同期を修正済み |
| D11 | 間合い・移動 | **大鎌はかなり広め／翅で飛行** | 詳細と初期値は `lab/BEELZEBUB.md` B2・B3 |
| D12 | 蠅 | **出さない** | パーティクル・音とも無し |

### D1 の実装上の帰結（重要）

同じコードを `lab/` と `index.html` に**2重に持たない**ため、次の形を取ります。

```
lab/core/*.js          ← Neural Core の唯一のソース（nodeでもブラウザでも動く素のJS）
      ↓  lab/inline.js
index.html の区画       ← 生成されたコピー（マーカーで囲ったブロック）
```

- **プレイヤー側にビルドは一切不要**です。`index.html` は今までどおり単体で開けば動きます。
- `lab/inline.js` は**私が使う道具**です。リポジトリに入りますが、出荷物には含まれません。

---

## 10. 実装計画（確定版）

| Phase | 内容 | ゲーム本体への影響 | 検証 |
|---|---|---|---|
| **1** | Mock Connectome / Graph / 圧縮 | **なし** | 決定論性・統計・圧縮後の到達性 |
| 2 | Neural Core（活性・抑制・減衰・再帰） | なし | NaN耐性、ノード数別実測表 |
| 3 | WorldState 境界 / Sensor 詰め替え | なし（読むだけ） | 既存AIが変わらないこと |
| 4 | **FairnessController（前倒し）** | なし | 難易度別の挙動 |
| 5 | Player Model（行動分類・n-gram・予測） | なし | 予測精度 |
| 6 | Tactical Brain / Action Selector | なし | Ablation A/B/C/D |
| 7 | 新規モブの身体・アニメ・音 | **ここから追加のみ** | スクリーンショット |
| 8 | Debug UI / Replay | 追加のみ | §30 の可視化 |
| 9 | Boss AI Lab（ヘッドレス戦闘シム） | なし | 勝率・生存時間 |
| 10 | 強化学習（オフライン） | なし | Ablation E |
| 11 | 本編統合・最適化 | 統合 | FPS影響実測 |

**Phase 1〜6 までは `index.html` に1行も触れません。** 既存の戦闘が壊れるリスクがゼロです。

---

*この文書は仕様書 §39 STEP 2 の成果物です。*
