# SERVER_MIGRATION_ANALYSIS.md

**対象**: CONTOUR — 市街戦（`lunex773-lab/ge-mu`、branch `claude/multiplayer-login-c9n7dk`）
**目的**: 「ゲームシステム Cloudflare 再構成・クライアント最適化 指示書」§1・§2・§42 の現状解析レポート
**日付**: 2026-09-23

> 指示書 §42 のとおり、**この分析の確認が取れるまでゲームのコードは変更しません。**
> この段階で追加したのは、計測用のスクリプト `lab/tools/profile_game.js`（出荷物ではありません）とこの文書だけです。
> 数値はすべてこのスクリプトで再現できます。

---

## 0. 結論を先に

1. **スマホの負荷の大半は、サーバーへ移せる処理ではありません。**
   JavaScript のいちばん重い処理は**街の交通**（車140台・歩行者237人）の更新で、1フレームの JS 時間の約3分の2を占めます。
   GPU 側では、**描画される約77万三角形のうち99.6%が視錐台カリングされていません**。建物の箱・信号機・街路樹などが、
   カメラの向きに関係なく街全体ぶん毎フレーム描かれています。どちらもクライアントに残る処理です。
2. **敵AIをサーバーへ移して軽くなるのは、ホスト役の1台だけです。** 実測では、裏世界でホストの JS 時間が 1.61 ms/フレーム、
   他のプレイヤーは 1.16 ms/フレームでした。ホスト以外は、今もAIを動かしていません。敵の負荷の大部分はアニメーションで、
   これは全員の端末に残ります。
3. **サーバー化の本当の価値は、正しさと公平さです。** 今は、誰でも公開ブローカーへ `{"t":"相手のID","d":99999}` を送れば
   相手を即死させられます（受信側が `applyDamage(msg.d)` をそのまま実行するため）。HP・位置・キル数も自己申告です。
   サーバー権威型にする理由の中心はここにあり、軽量化ではありません。
4. したがって実装は**2本立て**を提案します。
   - **A（クライアント最適化）**: サーバーと無関係にすぐ効く。
   - **B（Cloudflare 化）**: 段階的に権威を移す。

   指示書の Phase 番号はそのまま使い、A を B と並行して前倒しすることを提案します（§8）。

---

## ① 現在のアーキテクチャ

| 項目 | 実際 |
|---|---|
| 構成 | `index.html` 1ファイル（14,847行）。HTML + CSS + 1つの `<script>` の中の IIFE。**ビルドなし** |
| 外部依存 | Three.js r128（cdnjs）、mqtt.js v5（jsDelivr）、Google Fonts |
| ホスティング | 静的（GitHub Pages 等） |
| 通信 | **公開 MQTT ブローカー** `wss://broker.emqx.io:8084/mqtt`。トピック `contour2/<ROOM>/…` を 20種類 pub/sub |
| 権威 | **ありません。** 各端末が自分の位置・HP・キル数を送信（自己申告）します。敵はルーム内で**ID が最小の端末**（ホスト）が動かし、8 Hz で配信します |
| ループ | `frame(now)`（`requestAnimationFrame`）。dt は 0.05 秒で打ち切り。入力 → 移動 → 当たり判定 → 重力 → カメラ → 各 update → 送信 → `renderer.render` |
| 当たり判定 | 自前の AABB。建物はセル単位のバケット（`bBucket` / `cityNear`）。射線は自前のレイ（`rayCity`、Phase 11 で距離打ち切り済み） |
| ワールド生成 | 街・建物内部・裏世界の残骸・蔦は**固定シード**（`mulberry32`）で、全端末が同一です。交通（車・歩行者）、バナナ（回復アイテム）、コンパス、スポーン位置は `Math.random` で、**端末ごとに違います** |
| 保存 | `localStorage` の `contour.*` キー（名前・ルーム・音量・明るさ・画質・ボタン配置・ゲート状態・ヴェルゼブブの記憶） |
| 既存の最適化 | 動的解像度（FPS に応じて pixelRatio を4段階）、遠い交通は3フレームに1回更新、猿は75 m 超で非表示、犬のAI LOD、ゲートの詳細は45 m 以内のみ、ミニマップは10 Hz、ヴェルゼブブのアニメは距離で間引き |
| 同期 | 他プレイヤーは 120 ms 過去を補間表示（スナップショット補間。途切れたら推測航法）。敵は受信した値へ滑らかに寄せる |

### 通信の実測（2人・同じルーム）

| | 送信 | 受信 |
|---|---|---|
| ホスト | 3.7 KB/s | 5.0 KB/s |
| ホスト以外 | 1.2 KB/s | 5.0 KB/s |

自分の状態は 18 Hz（`PUB_MS` 55 ms、差分＋1秒ごとのキーフレーム）、敵のスナップショットは 8 Hz（JSON）で送っています。

---

## ② 現在の処理負荷（実測）

**計測条件**:
- この機械の headless Chromium。JS の時間は描画を止めて測り、描画の数字は1回だけ実際に描いて数えました。
- ソフトウェア GL なので **GPU 時間は測れません**。代わりに三角形数・ドローコール数を指標にします。
- 携帯はこの機械の 4〜8倍遅いと仮定しています（**実機では未計測**）。
- 再現: `node lab/tools/profile_game.js 20`

### 1フレームの JS 時間

| | 平均 | p95 | p99 | 携帯換算（×4〜8） |
|---|---|---|---|---|
| 表世界（1人） | 1.74 ms | 2.9 ms | 4.8 ms | 7〜14 ms |
| 裏世界（1人＝全ての敵を自分が動かす） | 1.81 ms | 3.1 ms | 5.4 ms | 7〜14 ms |

60 FPS の予算は 16.7 ms で、これに描画（GPU）が加わります。携帯では JS だけで予算の半分近くを使っている可能性があります。

### 処理ごとの内訳（µs/フレーム、この機械）

| 処理 | 表世界 | 裏世界 | 内容 |
|---|---|---|---|
| `updateCity` | **1,168** | 245 | 車140台・歩行者237人・猿の移動と、インスタンス行列の書き込み（裏世界では車が止まる） |
| `updateDogs` | 2 | **672** | デモドッグ約45頭。AI・歩行・ポーズ |
| `cityCollide` | 89 | **363** | 建物の押し出し。裏世界では1フレーム約9回（敵の分） |
| `updateBzb` | 3 | 103 | ヴェルゼブブ（AIはホストのみ） |
| `publishState` / `publishMobs` | 82 / 36 | 85 / 72 | JSON 化と送信 |
| `updateCombat` | 78 | 76 | 射撃・リロード・被弾 |
| `updateGorgons` / `updateFlayers` / `updateVecna` | 1 / 2 / 35 | 60 / 29 / 36 | |
| `updateMusic` | 42 | 43 | BGM（WebAudio のパラメータ更新） |
| `updateDayNight` | 29 | 17 | 昼夜のライティング |
| `updateMinimap`（`drawMap`） | 18 | 25 | 10 Hz で**毎回グリッドから描き直し** |
| `updateHud` | 11 | 11 | DOM |

### ホストとそれ以外の差（裏世界・2人・犬46頭）

| | 1フレームの JS | `updateDogs` | `updateGorgons` | `updateBzb` | `publishMobs` |
|---|---|---|---|---|---|
| ホスト（AIを動かす） | **1.61 ms** | 575 µs | 148 µs | 77 µs | 75 µs |
| ホスト以外（受信して表示） | **1.16 ms** | 355 µs | 122 µs | 6 µs | 6 µs |

→ **AIをサーバーへ移すと、ホストの端末が約 0.45 ms（携帯で約 2〜4 ms）軽くなります。ホスト以外はほぼ変わりません。**
犬のコストの約6割は、全員に残るポーズとアニメーションです。

### 描画（表世界、スポーン地点）

| 指標 | 値 |
|---|---|
| ドローコール | 73〜186（視点による） |
| **三角形** | **約 77万**。うち **77万（99.6%）はカリングされない InstancedMesh**（`frustumCulled = false` を16箇所で設定） |
| 最大の出どころ | 12三角形の箱のインスタンス（建物の壁・床・窓枠・設備など）で**約45万**。信号機（64三角形×484基×3色）で約9万、歩行者信号で約3.5万、車で約8万（車輪を含む）、並木で約1.7万 |
| メッシュ | シーン全体で1,264（InstancedMesh 92個・インスタンス 53,548）、描画対象 247〜332、SkinnedMesh 6 |
| シェーダープログラム | 10〜17 |
| **インスタンスバッファの毎フレーム再転送** | **20個・274 KB/フレーム**（60 FPS で約 16 MB/s。裏世界では30個・297 KB） |
| JS ヒープ | 約 30〜36 MB 使用 |
| GC | 全時間の約1% |

### 指示書 §2 の形式での整理

- **CPU負荷が高い処理**: 交通の更新（`updateCity`・`updateCar`・`updatePed`・`gapAhead`・`setInst`）。裏世界では犬（`updateDogs`・`dogStep`）と `cityCollide`/`cityNear`
- **GPU負荷が高い処理**: カリングされない街全体の InstancedMesh（約77万三角形）、毎フレームのインスタンスバッファ転送（274 KB）
- **メモリ負荷が高い処理**: 特に無し（ヒープ30 MB台、GC 1%）。ただし `dispose()` は5箇所しかない。ピア退出時は解放しているが、他の経路は未監査
- **JavaScript実行時間が長い処理**: 上表の順
- **毎フレーム実行されている処理**: 交通（近くのもの）、交通・群衆のインスタンスの `needsUpdate`、方位ストリップの transform、**透明化ボタンの `innerHTML` 書き換え**（`updateCombat` → `updateCloakBtn`、数字が変わらなくても毎フレーム）、BGM のパラメータ、昼夜の uniform、全建物マテリアルの uniform
- **毎フレーム実行する必要がない処理**: 遠い歩行者・車のポーズ（3フレームに1回まで落とし済みだが、転送は毎フレーム全量）、ミニマップの静的な格子（毎回描き直し）、`updateCloakBtn` の DOM、昼夜ライティング（数秒単位の変化）
- **Draw Callが多い箇所**: 少なめ（73〜186）。インスタンシングが効いており、問題は三角形数とカリング
- **Geometryが多い箇所**: `new THREE.*Geometry` が99箇所、マテリアルが94箇所（猿のアバターは1体ごとにマテリアルを新規作成）
- **Textureが重い箇所**: 画像テクスチャ無し（手続き的シェーダー）。名前札の CanvasTexture のみ
- **Collisionが重い箇所**: 裏世界の敵の `cityCollide`（1フレーム約9回・363 µs）。バケット分割は既にあり、全対全ではない
- **AIが重い箇所**: デモドッグ（ホストのみ AI 部分。ポーズは全員）
- **DOM処理が重い箇所**: `updateCloakBtn` が毎フレーム `className` と `innerHTML` を書き直す（レイアウトの再計算を招く）。方位ストリップは毎フレーム transform（向きが変わった時だけでよい）。FPS 表示は0.4秒ごとで問題なし。ヴェルゼブブのHPバーは Phase 11 で変化時のみに修正済み

---

## ③ サーバーへ移すべき処理

| 分類 | 処理 | 理由 / 今どうなっているか |
|---|---|---|
| **MUST SERVER** | HP・ダメージ・死亡・キル数 | 今は受信した `d` をそのまま適用するので、誰でも他人を即死させられる |
| MUST | 射撃の命中判定 | クライアントは「撃った（向き・時刻）」だけを送り、サーバーが自分の街と、遅延ぶん巻き戻した相手の位置でレイを判定する |
| MUST | 敵の HP・死亡・AI・位置（猿・デモドッグ・デモゴルゴン・マインドフレイヤー・VECNA・ヴェルゼブブ） | 今は「ID が最小の端末」がホスト。ID は自分で名乗れるので、誰でもホストになって敵を操れる |
| MUST | ゲート（裂け目）の状態・世界のキル数・レイジ・死体リスト | 今は端末間のゴシップで合意している（Phase 7 前に修正した部分） |
| MUST | ルームへの参加・退出・プレイヤー一覧 | ホスト選出を無くす |
| MUST | 入力・コマンドの検証（レート制限・サイズ上限・不正なコマンドの破棄） | §22 |
| **SHOULD SERVER** | 自分の位置 | 最初は**検証だけ**（速度・テレポート・壁抜け）にし、次の段階で予測＋補正を入れて権威化する（§7・§8） |
| SHOULD | アイテム（バナナ・腐ったバナナ・コンパス） | **今は端末ごとに位置が違います**（腐ったバナナだけ固定シード）。サーバー管理にすると全員共通になるので、**仕様変更**です（下記の確認事項） |
| SHOULD | 共有時計（交通信号・昼夜・ゲートの年齢） | サーバー時刻に合わせる |
| SHOULD | ヴェルゼブブの記憶（プレイヤーごとの癖）と、倒した記録 | 今はホスト端末の `localStorage`。ルームに保存すれば、誰がホストでも同じ個体になる |
| SHOULD | VECNA の「掴み」やノックバックによる位置変化 | 位置の権威化と一緒に移す |
| **CLIENT** | 描画・カメラ・入力・HUD・音・BGM・エフェクト・パーティクル・ミニマップ | |
| CLIENT | **交通（車・歩行者・フェラーリ）** | 今も端末ごとに乱数で別物で、見た目のための要素。全部を送ると帯域が大きすぎる。車に轢かれたダメージは「イベント」として送り、サーバーは妥当性だけ確認する |
| CLIENT | 全ての敵・他プレイヤーのアニメーションとポーズ、補間 | 実測で敵のコストの大部分 |
| CLIENT | 動的解像度・LOD・カリング・設定・ボタン配置 | |
| **SHARED** | 街の形（建物・内部・残骸・蔦を固定シードから生成）と問い合わせ（`clearAt` / `cityCollide` / `supportHeight` / `rayCity` / `rayWrecks`） | サーバーの判定とクライアントの予測が**同じ答えを出す**ために必須 |
| SHARED | プレイヤーの移動1ステップ（`EYE` `GRAV` `JUMP` `WALK` `RUN` `PRAD`） | 予測と補正が一致するため |
| SHARED | 武器の定数（`DMG` `FIRE_INTERVAL` `MAX_RANGE` 弾数・リロード） | 検証ルール |
| SHARED | 敵のシミュレーションコード | **オフライン・一人プレイでも今までどおり遊べるように**（§21）。同じコードをサーバーでもクライアントでも動かす |
| SHARED | プロトコル・スキーマ・乱数（`mulberry32`）・`lab/core`（Neural Core は既に node で動く） | |

## ④ クライアントに残すべき処理

指示書 §24 のとおりです: 描画・カメラ・入力・自分の移動の予測・補間・アニメーション・UI・音・エフェクト・パーティクル・LOD・カリング・見た目の演出。
このゲーム固有のものとして、加えて**交通（車・歩行者）**、**一人プレイ／オフライン時のローカルシミュレーション**（共有コードで動かす）、
**設定と端末固有の保存**（音量・明るさ・画質・ボタン配置）を残します。

## ⑤ 最適化すべき箇所（効果の大きい順・根拠付き）

| # | 箇所 | 実測 | 対策 | 期待 |
|---|---|---|---|---|
| 1 | **カリングされない InstancedMesh** | 77万三角形の99.6% | 街を区画（チャンク）ごとの InstancedMesh に分け、区画単位で視錐台＋距離カリング（霧で見えなくなる距離）。毎フレーム大量の `visible=false` を書く形にはしない（§14） | 三角形を数分の1に（視野角72°・霧の距離から見積もり。実測で確認する） |
| 2 | **交通の更新** | 1.17 ms/フレーム（表世界の JS の3分の2） | 距離で段階化（近: 毎フレーム / 中: 1/3 / 遠: 1/6＋ポーズ省略 / 視界外: 位置のみ）。行列の合成を軽くする（`setFromEuler` → 直接の四元数） | 0.4 ms 以下を目標 |
| 3 | **インスタンスバッファの毎フレーム全量転送** | 274 KB/フレーム（約16 MB/s） | 変わった範囲だけ転送（`updateRange`）。近くのものを前に詰めて、範囲を小さく保つ | 転送量を数分の1に |
| 4 | 裏世界の敵の当たり判定 | `cityCollide` 363 µs・約9回/フレーム | 静止している敵は判定を省く。セルの候補をキャッシュする | 半分程度 |
| 5 | ミニマップ | 10 Hz で格子ごと描き直し | 静的な層（格子）をオフスクリーンに1回だけ描き、動く点だけ上書き | 小さいが確実 |
| 6 | 毎フレームの DOM | `updateCloakBtn` の `innerHTML`、方位ストリップ | 値が変わった時だけ書く | 小（携帯ではレイアウト再計算が効くことがある） |
| 7 | 猿のアバター | 1体ごとにマテリアルを新規作成 | マテリアルの共有 | メモリとプログラム切り替え |
| 8 | 解放漏れの監査 | `dispose()` は5箇所 | ピア・名前札・一時メッシュの経路を監査 | メモリの長時間安定 |
| 9 | 敵のAI（ホストのみ） | ホストで +0.45 ms | サーバーへ（B） | ホストだけ |

**既にあるので作り直さないもの**: 動的解像度（§26。UI は DOM なので影響なし）、ピアの補間（§8）、交通・猿・犬の距離 LOD の一部。

## ⑥ Cloudflare 構成案

```
 スマホ（index.html のまま・ビルド不要）
   │  WebSocket（入力・コマンド 20〜30 Hz、まとめ送り）
   ▼
 Worker（ルーティング・Origin 検査・ルーム名の検証）
   │  env.ROOMS.idFromName(room) / get(id, { locationHint: "apac" })
   ▼
 Durable Object「GameRoom」（SQLite バックエンド）
   ├ tick 20 Hz: 入力の適用 → 検証 → 敵AI → 戦闘 → アイテム → イベント
   ├ スナップショット 10〜15 Hz: 近くのものだけ（関心範囲）＋差分、シーケンス番号・サーバーtick
   ├ ルームの永続状態（ゲート・倒した記録・ヴェルゼブブの記憶）は DO の SQLite ストレージ
   └ 誰もいなくなったら tick を止める（→ 休止して課金も止まる）
 D1（必要になった段階で）: プレイヤーの統計・進行など、ルームをまたぐデータ
```

- **ディレクトリ構成**（§33 を盲目的には作らず、今の形に合わせます）:
  - `server/`（Worker と GameRoom、`wrangler.jsonc`、`package.json`）
  - `shared/`（ワールド・移動・プロトコル・検証・敵のシム。素の JS で、node・Workers・ブラウザのどこでも動く）
  - `index.html` は1ファイルのまま。`shared/` は `lab/inline.js` と同じ方式で生成ブロックとして埋め込みます。この方式は Neural Core で実績があります。
- **フォールバック**（§21）: サーバーに繋がらなければ、今までどおり一人プレイ（共有コードをローカルで実行）で遊べます。
  移行期間中は MQTT 経路もスイッチで残し、サーバー経路が安定したら公開ブローカーを外します。
- **Cloudflare の現行仕様**（公式ドキュメントのソース `cloudflare/cloudflare-docs` を本日読んで確認。developers.cloudflare.com はこの環境のネットワーク方針で遮断されていたため）:

| | Workers Free | Workers Paid（月 $5〜） |
|---|---|---|
| DO リクエスト | 100,000/日（**受信 WebSocket メッセージは 20通で1回**として計算） | 100万/月 ＋ $0.15/100万 |
| DO 稼働時間 | 13,000 GB-s/日（128 MB で **約29ルーム時間/日**） | 40万 GB-s/月 ＋ $12.50/100万 GB-s |
| DO のストレージ | SQLite のみ。読み 500万行/日・書き 10万行/日・5 GB | 読み 250億行/月・書き 5,000万行/月 込み |
| CPU/呼び出し | **Workers と同じ 10 ms**（DO の FAQ に「Workers と同じ呼び出しあたりの CPU 上限」とある） | 既定 30秒（最大5分） |
| 休止 | `setTimeout`/`setInterval`/アラームがあると休止しない → **tick 中は稼働時間として課金** | 同じ |

  - 見積もり（4人・1日2時間・入力 20通/秒/人）: 受信 57.6万通/日 → 請求 2.9万リクエスト/日、稼働 900 GB-s/日。**Free の枠に収まります。**
  - 上限の目安（Free）: リクエストで約27人時/日、稼働時間で約29ルーム時間/日。
  - **注意**: Free の 10 ms/呼び出しは、20 Hz で世界全体を回す部屋には厳しい可能性があります。開発・検証は Free で行い、本番は Paid を推奨します。
    実際の挙動はデプロイして確認します。

## ⑦ 変更によるリスク

| リスク | 内容 | 対策 |
|---|---|---|
| 操作感の悪化 | 予測とサーバーの計算がずれると、位置が引き戻される（ラバーバンド） | 移動を**共有コード＋固定タイムステップ**にする。補正は滑らかに。最初は「検証のみ」で始めて段階的に権威化 |
| 射撃の当たりが悪くなる | サーバーは遅延の分だけ古い相手を見ている | ラグ補償（相手の位置を RTT/2＋補間遅延ぶん巻き戻して判定、上限付き） |
| 大規模な分離作業での回帰 | 14,847行・1ファイル。6種類の敵の AI と描画が同じ関数に混ざっている | 1種類ずつ分離する。既存のゲーム内テスト（Phase 3/7/ゲート同期/Phase 11）に加え、「同じシードならサーバーとローカルで同じ結果」のテストを足す |
| 経路が2つになる | サーバー経路と一人プレイ経路の両方を保守する必要がある | 共有コード化で「同じ関数を2か所で呼ぶ」形にする |
| 上限・費用 | Free で上限を超えると操作がエラーになる | 失敗したら一人プレイへ退避。利用量を F3 に表示 |
| 遅延 | DO は1か所に置かれる。`locationHint: "apac"` で日本付近に寄せるが保証ではない | 海外の参加者は 150〜250 ms を想定 |
| **仕様が変わる点** | アイテムが全員共通になる、ホスト選出が無くなる、ヴェルゼブブの記憶がルーム単位になる | **下の確認事項** |
| デプロイ権限 | あなたの Cloudflare アカウントが必要 | ローカル（workerd）での構築とテストは私が行える（npm に到達可能、wrangler 最新版 4.136.3 を確認）。本番デプロイには API トークンの登録か、あなた自身の `wrangler deploy` が必要 |
| 交通は同期されないまま | 車に轢かれる判定は端末ごと | 見た目の要素として割り切る（全部を送ると帯域が大きすぎる） |

## ⑧ 実装 Phase（提案）

指示書 §35 の番号を使います。**A（クライアント最適化）はサーバーに依存しないので、B と並行して先に着手する**ことを提案します。
実測では、スマホが軽くなる効果の大半は A にあるためです。各 Phase は §36 の手順（現状確認 → 計画 → 実装 → エラー確認 → 設定確認 → 起動 → 機能 → 性能）で進め、
前後の数値は `lab/tools/profile_game.js` で比べます。

| Phase | 内容 | 完了の条件 |
|---|---|---|
| 1 | 現状解析（本書） | ✔ |
| **B-2** | `server/` と `wrangler.jsonc`。空の Worker と GameRoom。ローカルの workerd で自動テスト | ローカルで WebSocket が張れる |
| B-3 | GameRoom: 参加・退出・tick の開始と停止（無人で休止） | 無人で tick が止まる |
| B-4 | クライアントの WebSocket 経路（スイッチ付き）、接続状態表示（接続中 / 再接続中 / 切断 / 再接続済み）、失敗時は一人プレイ | サーバーを止めてもゲームが止まらない |
| B-5 | 状態の同期: 今の MQTT のメッセージをサーバーが中継・集約（シーケンス番号・時刻・サーバーtick、形式・サイズ・レートの検証） | 2人で今と同じに遊べる |
| B-6 | 権威化①: HP・ダメージ・死亡・キル数・命中判定（ラグ補償）・アイテム | `hit.d` を誰も偽れない |
| B-7/8 | 移動の予測・補正（共有の移動コード）。位置の検証 → 権威化 | ラバーバンドが体感で出ない |
| B-9 | 敵をサーバーへ1種類ずつ: ヴェルゼブブ（既にモジュール化済み）→ 猿 → デモドッグ → デモゴルゴン → マインドフレイヤー → VECNA。ゲートと世界の状態も | 同じシードで、ローカルとサーバーのシムが一致 |
| **A-10/13** | 区画ごとのインスタンシング＋視錐台・距離カリング | 三角形数・ドローコールを実測で比較 |
| A-12 | 交通・群衆の LOD 段階化、インスタンスの差分転送 | `updateCity` と転送量を実測で比較 |
| A-11 | 裏世界の敵の当たり判定 | `cityCollide` を比較 |
| B-14 | 通信の最適化（関心範囲・差分・コンパクト形式） | KB/s を比較 |
| A-15 | メモリ（`dispose` 監査）・音（ノードの再利用）・ミニマップ（静的層のキャッシュ） | 長時間プレイでヒープが増えない |
| 16 | F3 にプロファイラ（FPS・フレーム時間・JS 時間・ドローコール・三角形・テクスチャ・オブジェクト数・可視数・ヒープ・RTT・メッセージ/秒・バイト/秒・サーバーtick） | 開発モードのみ表示 |
| 17 | **実機テスト** | **あなたのスマホでの計測が必要です**（私には実機がありません） |
| 18 | 最終調整 | |

---

## 決定事項（ユーザーの回答、2026-09-23）

1. コードと構成は私が作り、Cloudflare の設定はユーザーが私の手順に従って行う
2. アイテム（バナナ・コンパス）は全員共通にしてよい
3. **A → B の順**で進める
4. 公開 MQTT ブローカーは外してよい
5. `index.html` の置き場所は、効率などを考えて私が決める → **Cloudflare（Workers の静的アセット）**。
   WebSocket と同じオリジンになるので Origin 検査が単純で、デプロイも1回で済む（B で詳細）

---

## 変更ログ（§40: 「Serverへ移した処理」「Clientに残した処理」「最適化した処理」）

### A（クライアント最適化）— 完了

**Serverへ移した処理**: なし（B で行う）
**Clientに残した処理**: すべて（A は描画と更新の書き方だけを変更）

**最適化した処理**:

| # | 内容 | 検証 | 効果（この機械） |
|---|---|---|---|
| A1 | 街の静的インスタンスを材質・形状ごとに統合し、3万三角形以上の8グループを4象限に分けて視錐台カリング | `lab/tools/view_cost.js`（320視点）、`lab/tools/pixel_diff.js`（8視点で**街はピクセル単位で同一**） | 街の三角形 647k → 542k（横持ち）/ 499k（縦持ち）、ドローコール 44 のまま |
| A2 | 交通・群衆の行列を直接書き込み、車間の判定を車線ごとのリストに、カメラの後ろは4フレームに1回・30〜90 m の歩行者は2フレームに1回（経過時間で進める）、裏世界では群衆を停止 | `lab/test/phase12.test.js`（行列は Object3D と一致、車間は全車走査と一致） | `updateCity` 1,297 → 845 µs（表）、278 → 128 µs（裏） |
| A3 | 近傍セルの建物リストを起動時に1回だけ作成、壁全体が届かない建物を飛ばす | `lab/test/collision.game.test.js`（2万地点で押し出し・床の高さが完全一致） | `cityCollide` 379 → 22 µs（裏）、犬 743 → 472 µs |
| A4 | 透明化ボタン・方位表示の DOM を変化時のみ更新。長時間プレイでの解放漏れを検査 | `lab/tools/soak.js`（3分・約5,000回の射撃・参加と退出の繰り返しで、ジオメトリ・マテリアル・テクスチャ・オブジェクト・DOM は増えない） | `updateCombat` 94 → 11 µs |
| A5 | F3 のプロファイラ（フレーム時間・JS 時間の内訳・オブジェクト数・ヒープ・メッセージ数/秒・サーバーtick）。**スマホでは `?debug` を付けて開く** | ゲーム内で表示を確認 | 無効時の負荷ゼロ |

**全体（`lab/tools/profile_game.js`、A の前 → 後）**:

| | 1フレームの JS 平均 | p99 |
|---|---|---|
| 表世界 | 2.03 → **1.37 ms（−33%）** | 6.6 → 4.6 ms |
| 裏世界 | 2.05 → **1.41 ms（−31%）** | 5.6 → 4.2 ms |

（計測ごとに ±10% 程度ぶれます。）

**試して採用しなかったもの**（数字で判断）:
- 街をもっと細かく分割するカリング: 三角形1つあたりの削減とドローコール1つの増加がほぼ釣り合い（約3千三角形につき1コール）、スマホで得になる根拠がないため保留。実機の数字（Phase 17）で再判断
- ミニマップの格子のキャッシュ: ここではコピーの方が遅かった（21 → 30 µs）ため元に戻した
- 猿のアバターのマテリアル共有: 自分のアバターは透明化でマテリアルを書き換えるため、共有すると全員が透ける。シェーダープログラムは既に共有されており、得るものが小さい

**残っているもの**:
- インスタンスバッファの毎フレーム転送（274 KB/フレーム）。更新する個体が配列上に散らばっているため、部分転送が効かない。実機で問題になるかを見てから判断
- GPU の実測（フィルレート・頂点処理）は実機でしか取れない（Phase 17）

### B（Cloudflare 化）— 途中（B1〜B4、B5 のうちアイテム）

構成: `server/`（Worker と GameRoom）、`shared/`（ゲームとサーバーが同じものを使う: 街の形・ルール・アイテム配置）、
`wrangler.jsonc`、`.github/workflows/deploy.yml`、手順は `SETUP.md`。ゲームは今も `index.html` 1ファイルで、ビルド不要です。

**Serverへ移した処理**:

| 処理 | 以前 | 今（ルームサーバー上） | 検証 |
|---|---|---|---|
| プレイヤーの ID とホスト | 各自が名乗る。最小 ID の端末がホスト（ID を選べばホストを奪えた） | 部屋が参加順に付ける。最古参が最小 ID＝ホスト | `server/test/room.test.js` |
| 送信者 | 名乗れば誰にでもなれた | 部屋が全メッセージに本当の送信者を押印 | 同上 |
| 対人の命中 | 被弾側が「受けた」と言われた通りのダメージ（`d: 99999` で即死） | 部屋が判定: 双方生存・同じ側・連射間隔・射程 300 m・**街を通した射線**（過去 0.7 秒の位置、脚・胸・頭）。通れば 20 ダメージ | `room.test.js`、`lab/test/server.game.test.js`（実際に狙って撃った弾も通る） |
| HP・死亡・キル | 各自が自己申告 | 部屋の記録。死亡とキルの帰属（直近 12 秒に撃った人）を部屋が決める。キルの自己申告は無効 | 同上 |
| アイテム（バナナ・腐ったバナナ・コンパス） | 端末ごとに別の場所（乱数）、各自で取って各自で復活 | **全員共通の配置**（`shared/items.js`）。最初に届いた人に部屋が渡し、回復も部屋が行い、復活（22 秒・45 秒）を全員に告げる | `server/test/relay.test.js`（時計を進めて復活時刻を確認）、`server.game.test.js` |
| 敵のスナップショット | 誰でも送れた | ホストのものだけ中継 | `room.test.js` |
| 通信量の制限 | なし | 種類ごとのレート制限、1通 16 KB まで、未知の種類は破棄 | `room.test.js` |

**Clientに残した処理**（今の段階）:
- 自分の移動（位置は自己申告のまま。検証は次の段階）
- 敵の AI（ホストの端末。部屋はホスト以外の報告を受け付けない）
- 車や敵から受けるダメージ（自分に対してだけ申告でき、部屋が記録する。敵がサーバーに移るまでの暫定）
- 描画・入力・音・UI・補間・アニメーションはすべてクライアント（§24 のとおり）

**フォールバック**: サーバーにつながらなければ（Cloudflare 以外に置いたコピー、ファイルを直接開いた場合、その日の無料枠を使い切った場合）、
ゲームは**1人で**続き、つながり直しを試し続けます（一度もつながっていなければ最大 30 秒おき、切れた場合は最大 15 秒おき）。
サーバーが戻れば自動で部屋に入る（`lab/test/solo.game.test.js`）。**公開 MQTT ブローカーは 2026-09-24 に外した**（誰でも何でも送れてしまうため。
2台での確認のあと）。

**負荷と料金に効く工夫**:
- 静的ファイル（ゲーム本体）は Cloudflare の無料・無制限の配信
- 部屋は WebSocket Hibernation API で、誰も何も送っていないあいだは課金されない。HP とスコアは接続ごとの添付データに保存し、起きたときに戻す
- 射線判定用の街は、建物の配置だけを先に作り（約 1 ms）、各建物の内部は射線が初めて届いたときに作る
  （全部を作ると 45 ms かかり、Free プランの 1 回あたり CPU 上限を超えうるため）。2 万本の射線で全体版と一致

**本番への反映**: **公開済み — <https://contour.lunex773.workers.dev>**（2026-09-24、GitHub Actions から初回デプロイ）。
以後はこのブランチへの push ごとに、サーバーのテスト → デプロイ → **本番サイトの確認**（`LIVE_URL` を付けた
`server/test/room.test.js`: 配信されているページがこのコミットの `index.html` と同一か、本番のルームで同じ 22 項目が通るか。
確認用のルームは `_check-` で始まる名前で、待ち時間は実測の往復時間に合わせて伸ばす）の順に自動で行う。

**残っているもの**:
- 移動の検証 → 速度・瞬間移動は済み（B-7/8）。壁抜けの検証と、裂け目の通過の距離はまだ
- 敵の AI をサーバーへ（ヴェルゼブブ → 猿 → デモドッグ → デモゴルゴン → マインドフレイヤー → VECNA の順）
- ~~公開 MQTT ブローカーの撤去~~ → 済み。テスト用のハーネスは偽の MQTT をやめ、ページの WebSocket を Playwright の `routeWebSocket` で受けて
  **本物のルームのコード（`server/relay.js`）**で中継するようにした。以後、ゲームのテストはすべて本番と同じ判定を通る
  （ID は参加順、ホストは最古参、命中はルームが判定）。ID を自分で決めていたテスト（裂け目の同期 S4・S5、phase7、soak）は参加順に合わせた

### Phase 17 実機テスト — 2回目（2026-09-24、iPhone 14 ともう1台、本番、同じルーム）

| | 縦持ち | 横持ち |
|---|---|---|
| 接続 | **ONLINE · 2**、往復 73〜108 ms | ONLINE · 2、往復 48〜108 ms |
| FPS | 60（最悪 18.0 ms） | 60（最悪 17.0 ms） |
| JS（ロジック / 描画） | 1.2 / 1.6 ms | 1.4 / 1.8 ms |
| ドローコール / 三角形 | 144 / 526k | 116 / 425k |
| 通信（ホスト側） | 送信 13.5 KB/s・22 通/s、受信 1.6 KB/s・13 通/s | 送信 13.5 KB/s・22 通/s、受信 2.0 KB/s・17 通/s |
| 相手の補間 | 1/1 | 1/1 |

**わかったこと・直したこと**:
- 2台で本番のルームにつながり、互いが見え、補間も効いている。2人になっても 60 FPS のまま
- **弾を増やしてほしい**（ユーザーの要望）→ 予備の上限 240 → **600**（マガジン 20 本分）、開始時 120 → **300**、
  バナナ +60 → **+90**、復活時は「+90」から「**最低でも開始時の 300**」に。マガジンは 30 発のまま（リロードのリズムを変えないため）
- **相手の名札が大きすぎた**: 世界の中の高さ 1.4 m・幅数 m の板で、近いと名札が相手の体を覆い、HP バーも隠していた。
  → 名札と HP バーを**画面上で一定の大きさ**（名札の高さ 17 px、バー 46 × 5 px）にし、頭の上に「バー、その上に名前」と積んだ。
  スコープで拡大しても大きさは変わらない
- **縦持ちで「NET ONLINE · 2 · 108ms」がミニマップにかかっていた**（1回目は 1人・往復表示なしで測っていたため見逃した）。
  → 名前の一覧を、画面の中央ではなくミニマップと歯車のあいだの中央に置いた。`hud_layout.js` は 4人・往復表示・長い名前の一覧で測るようにした（0 組）
- 送信 13.5 KB/s（1時間に約 49 MB）の大半は敵のスナップショット。B-14（関心範囲・コンパクト形式）で減らす → 下の B-14 で実施

### B-14 通信の最適化 — 関心範囲（2026-09-24）

`lab/tools/net_cost.js`（2人・同じ場所、種類ごとの送信量とスナップショットの内訳）で測ると、裏世界のホストは **11.8 KB/s・23 通/s**
（実機の 13.5 KB/s とほぼ同じ）。スナップショット 1,469 バイトのうち **犬 998 バイト（68%）**、猿 250、死体 25〜100。

| 変更 | 内容 |
|---|---|
| 犬 | 8 回に 1 回（1 秒に 1 回）は全頭（`kf: 1`）。その間は、**裏世界の相手から 110 m 以内**の犬と、**HP・状態・印が変わった犬**だけ（`kf: 0`）。受け手は全頭の回だけ「いない犬」を消す |
| 猿と死体 | 表世界に誰もいないときは、全頭の回にだけ載せる（戻ってくる人には、裂け目の暗転のあいだに届く） |
| 新しい人が来たとき | その場で全頭の回を送る |

110 m は、犬の霧（密度 0.008）で見え方が約半分になる距離（150 m でほぼ見えない）。

| | 前 | 後 |
|---|---|---|
| 裏世界・ホストの送信 | 11.8 KB/s | **4.4 KB/s（−62%）** |
| 表世界・ホストの送信 | 3.6 KB/s | 3.6 KB/s（変わらず） |
| 通数 | 23 通/s | 23 通/s（変わらず） |

`lab/test/snapshot.game.test.js`: 相手の犬はホストと同じ頭数で欠けなし、近くの犬のずれ 0.2 m 以内・遠くの犬 0.6 m 以内（1 秒遅れまで）、
414 m 先の犬の死も 132 ms で届く、表世界に戻ると猿 12 匹がすぐそろう。

**公開後の確認の改善**: 公開直後は新しいページが行き渡るまで少しかかる（一度、前の版を読んで失敗した）。ページも最大 2 分、5 秒おきに確かめる。

### B-7/8 移動の検証（2026-09-24）

ルームのサーバーが、プレイヤーの位置の申告を1歩ずつ確かめる（`server/move.js`）。

- **ルームの時計で「走ってよい距離」を貯める**: 走る速さ（`shared/rules.js` の `RUN` 10.2 m/s）の 1.25 倍で貯まり、4 秒分まで。1歩ごとに進んだ分を使う。
  通信が止まってまとめて届いても引き戻さず、速度を偽っても長く見れば走る速さを超えられない
- **ルームに見えない力の分**（敵の押し出し・引き寄せ、車）: 別枠。表世界 12 m、裏世界 40 m まで（マインドフレイヤーの衝撃波 11 m、VECNA の引き寄せ最大約 30 m）
- **自由に動いてよい**: 参加直後（とルームが眠りから覚めた直後）、復活直後、裂け目の通過（1.5 秒に 1 回まで）。
  裂け目の位置はルームが知らない（誰でも告げられる）ため、通過の移動距離はまだ確かめていない。裂け目がルームのものになった時点で確かめる
- **超えたとき**: その位置は他の人に伝えず、当たり判定にもアイテムにも使わない。本人には最後に認めた位置（`pos`）を返し、ゲームがそこへ戻す（1 秒に 2 回まで）。
  `?debug` の NET 行に戻された回数（`fix`）を出す。**普通に遊んで 0 のままか**を実機で見る
- 街の外（±336 m 超）は不可

| 確認 | 結果 |
|---|---|
| `server/test/relay.test.js`（偽の時計） | 30 秒走り続けて 0 回、3 秒止まってまとめて届いても 0 回、150 m の瞬間移動は戻して伝えない、2 倍速は 5.6 秒で捕まる、裏世界の 80 m は可・表世界は不可、裂け目・復活は自由、すぐ戻る通過は不可、街の外は不可、戻す連絡は1回（27/27） |
| `server/test/room.test.js`（workerd、本番の確認でも） | 街を横切る瞬間移動は伝わらず、元の位置が返る（23/23） |
| `lab/test/move.game.test.js`（本物のゲーム） | 20 秒・174 m 走り回って 0 回、裂け目・押し出し 11 m・引き寄せ 28 m・復活で 0 回、200 m の瞬間移動は戻され相手にも見えない、3 倍速は戻される（9/9） |

テストで場面を作るために瞬間移動させるもの（ハーネスの既定、`server.game.test.js` は `--var MOVE_CHECK:off`）以外、**本番の部屋はすべて検証オン**。

### B-9 敵をサーバーへ ① ヴェルゼブブ（2026-09-24）

**方針**（ユーザーの質問「ホストとゲストの差をなくせるか」への答え）: 1人のときは今どおり端末で動かし、**2人以上のときはルームが動かす**。
ホストだけが敵を動かして送る形をやめ、全員が同じ情報を受け取る。1種類ずつ移す。最初がヴェルゼブブ。

| 段 | 内容 | 確認 |
|---|---|---|
| 1 | 脳（神経回路）の組み立てが初回 15〜19 ms かかり、無料プランの 1 回 10 ms を超える → 構築結果を公開前に計算して `server/brain.js` に書き出し（`NeuralCore.prepared()` / `fromPrepared()`）、サーバーは読み込むだけ: **0.42 ms** | `server/test/brain.test.js`: ゲームと同じ神経回路・学習済みの読み出し、400 ステップで全ニューロン・全得点が一致 |
| 2 | 街の「建物の外か」「屋根の高さ」を共有（`shared/city.js` `footprints()`） | `lab/test/city.test.js`: 2 万か所でゲームと一致 |
| 3 | ヴェルゼブブの頭と体を動かす部分を1つのモジュールに（`lab/core/boss_runner.js`）。ゲームもこれで動かす | `phase7.game.test.js` 16/16、ラボの単体テスト全部 |
| 4 | ルームが動かすヴェルゼブブ（`server/boss.js`）と、受け渡し（`server/creatures.js`） | 下 |

**受け渡し**（二重に動かさず、止めもしない順番）:
- 2人目が来ると、ルームはホストの次のスナップショット（1/8 秒以内）で彼の状態を受け取り、**それから**「ルームが動かす」（`own { b: 1 }`）と全員に告げる。
  ホストの端末は止まり、全員がルームの `sv`（1 秒に 10 回）から描く
- 1人に戻ると、ルームは最後の状態を添えて返す（`own { b: 0, s }`）。残った人の端末がそこから続ける
- 接続が切れた端末は、自分で動かす

**ルームでの彼**: ルームが知っていること（各プレイヤーの位置・向き・HP・透明化、共有の街）で、ゲームと同じ頭と体を動かす。
メッセージが届くたびに 40 Hz で進める（感覚 20 Hz・思考 10 Hz がスマホと同じになる）。アラームは使わない（料金の対象で、眠れなくなるため）。
彼への命中はルームが判定する（撃った人が生きていて表世界、連射間隔、射程、すね・胸・頭への視線）。倒れた裂け目では立ち上がらない（B7。ホストの記録も `bs` で受け取る）。
彼の刃が当たったかは、今までどおり各端末が自分で判定する。

**処理の重さ**（`server/test/boss.test.js`、新しいプロセス＝温まっていない状態）: 1 回の処理 **平均 0.08 ms**。最初の数回（神経回路、心、立ち上がり、最初の感覚と思考）は
それぞれ別の回に分け、どれも数 ms（テストの上限は 9.5 ms）。1分間で約 600 回考え、攻撃もする。

**確認**: `lab/test/boss.game.test.js`（本物のゲーム、ルームの本物のコード）11/11 — 1人のうちは端末が動かす／2人目でルームが**誤差 0.03 m** で引き継ぎ、
ホストは送らなくなる／両方で同じ位置／両方の弾がルームの判定で当たる／近くのプレイヤーを斬る／1人に戻ると最後の状態のまま返る／また来ると引き継ぐ／
全員の画面で倒れ、同じ裂け目では立ち上がらない。

**途中で見つけて直したこと**: 参加・退出のときにルームが出す送信（持ち主の交代など）が、どこにも流れていなかった（`room.js` とハーネス）。
ハーネスでページを閉じても、ルームに退出が伝わっていなかった（本物は伝わる）。

**残り**: ~~彼の学んだことはルームが生きている間だけ~~ → 下の B-9+ でサーバーに保存。ルームが眠ると（全員 10 秒以上無操作）戦いの途中の状態は失われる。
彼の刃の当たりもルームが判定する形（今は各端末）。次は猿 → 犬 → デモゴルゴン → マインドフレイヤー → VECNA。

### B-9+ BEELZEBUB は戦いの中で学び、その記憶はサーバーにある（2026-09-24）

表記は英語（ボスバー・倒れたときの通知とも **BEELZEBUB**）。

**記憶の置き場所**: 全ルーム共通の Durable Object **BossMind**（`server/mind.js`、ロジックは `server/mindstore.js`、`wrangler.jsonc` の `MIND`・migration `v2`）。
ルームは各メッセージの後、記憶への用件をまとめて 1 回で送り（`relay.takeAsks` → `ask`）、答えはルームのメッセージとして流れる（`mindSaid`）。
届かないとき（バインディングなし・通信失敗）は、手元の読み出しのまま戦い続ける。ライブ確認のルーム（`_check-…`）は別の記憶を使うので、テストで彼が学ぶことはない。

| 記憶するもの | 中身 | 規則 |
|---|---|---|
| 各プレイヤーについて | ゲームが作るプレイヤーモデル（`player_model.js`、約 2.5 KB）と、その人に対して公平さが落ち着いた位置。**頭上の名前**で保存 | 一番新しいものを残す。最大 400 人（一番長く会っていない人から忘れる）。ルーム内の ID・名前なし・大きすぎる・壊れたものは保存しない |
| 自分の心の読み方（読み出し） | 運動ニューロンの重み 91 ＋ 行動ごとのバイアス 10 ＝ 101 個（ラボが学習させた部分、`lab/learn/train_readout.js`） | 下の「戦いの中の学習」 |

**戦いの中の学習**（ラボと同じ進化戦略を、本物の戦い 1 回を 1 試行として）:
- 戦いは「候補」で行う。読み出しをランダムな方向 ε に少し（σ = 0.05）ずらしたもの。同じ ε の＋と−を 2 回の戦いに配る（対）
- 採点は動かした側で（`boss_runner.js` `fightScore`）: ラボの報酬のうち見えるもの — 刃が当たった +10・背後から +8・予測どおり +15・空振り −10 — から、失った HP（全部で −100）と倒れたこと（−50）を引き、**戦っていた 1 分あたり**。20 秒未満の戦いは採点しない（候補は配り直し）
- 対の 2 戦がそろうと、どちらの向きが良かったかを記録。**4 対ごとに**まとめて一歩進む（101 次元では 1 対の向きはほぼノイズのため）
- 本物の戦いは相手も場所も違ってばらつくので: 差はいつもの大きさで割って ±2 に制限（1 回の極端な対で大きく動かない）、短い戦いは軽く、1 歩で 1 つの数は最大 0.05、
  **ラボの読み出しから 1.5 以上は離れない**（101 個まとめて）— 何が起きてもラボで確認した BEELZEBUB の範囲にとどまる
- 12 戦ごとに平均を取り、最高を記録。明らかに（標準誤差の 3 倍）悪い 12 戦が続いたら、最高の読み出しに戻す
- 報告されなかった候補は 20 分後に配り直す。ラボが学習し直すと（新しい `brain.js`）、そこから学び直す
- わざとゆっくり: 1 歩に 8 戦、目に見える変化には数十歩

**誰が何を言えるか**（改ざん対策）:
- **ルームが動かす戦い**（2 人以上）: ルームが自分の候補で戦い、採点し、報告する。戦った相手のモデルも保存
- **1 人で遊ぶ戦い**: 端末が、ルームから渡された**自分の候補**でだけ報告でき、1 回限り、ここにいた時間より長い戦いは認めない。
  保存できるのは**自分のモデルだけ**（ルームが知っている名前で）、`PlayerModel.restore` が受け付ける形のものだけ
- 端末は来たときに「今の読み出し・候補・自分について知っていること」を受け取る（`mind`）。読み出しは端末に保存し、サーバーがないときも使う。
  自分のモデルは、サーバーの方が新しければ（他の端末やルームが戦った）取り込む。ルームが彼を返すときも、ルームが学んだその人のモデルを渡す

**見える場所**: `https://…/mind`（JSON: 何歩進んだか・何戦・最高に戻った回数・ラボからの距離・覚えている人数。名前は出さない）。F3 の BOSS NEURAL DEBUG に `LEARN` 行（候補・読み出しの歩数・この戦いの秒数と点）。

**確認**:
- `server/test/mind.test.js` 24/24 — 保存と新旧の比較、候補の対、4 対で一歩、1 歩の上限、**目標の読み出しに近づく**（1.0 → 0.41）、どれだけ押されても 1.5 で止まる、悪い 12 戦で最高に戻る、作り直しても続きから、ラボが変わればやり直し
- `server/test/learn.test.js` 14/14 — ルームと記憶を合わせて: 来たときに渡すもの、他人の候補・二重報告・ここにいた時間より長い戦いは拒否、自分のモデルだけ保存、
  ルームの戦い（候補・2 人のモデルと公平さの平均で始まり、倒れると報告、2 人とも保存）、返すときにモデルを渡す
- `server/test/room.test.js`（ローカル workerd と本番）— 来たときの `mind`、自分の候補の報告と次の候補、`/mind` が名前を出さないこと
- `lab/test/mind.game.test.js` 11/11（本物のゲーム）— 1 人で候補を使って戦い採点、2 人目でルームが引き継ぐと報告し自分のモデルが保存される、
  ルームも候補で戦う、返されると続きを自分の候補で、サーバーで進んだ読み出しが後から来た端末に届いて使われ、端末に残る

### B-9 敵をサーバーへ ② サルの群れ（2026-09-24）

ヴェルゼブブと同じ形: 1人のときは端末、**2人以上ならルームが動かす**。

| 段 | 内容 |
|---|---|
| 1 | サルの群れの動き（歩道のグラフを信号どおりに歩く・死体を食べに行く・逆上して一番近いプレイヤーへ走る・控えの 10 匹が角から現れる・倒れた猿が起き上がる・60 秒で静まる）を **`shared/troop.js`** に切り出し。信号（グリーンウェーブ）とグラフの歩き方も同じファイル。ゲームもこれで動かす（`lab/inline.js` が SHARED ブロックに入れる） |
| 2 | ルームが動かす群れ **`server/troop.js`**。ルームが知っていること（各プレイヤーの位置・倒れているか・共有の街）で 20 Hz |
| 3 | 受け渡し（`server/creatures.js`）: ヴェルゼブブ（`b`）と群れ（`m`）を別々に持つ。2人目が来たらホストの次のスナップショットから引き継ぐ（群れは「誰かが表世界にいるか 8 回に 1 回」しか載らないので、9 回載らなければホストは動かしていない → ルームが新しい群れで始める）。1人に戻ると最後の状態を添えて返す（`own { m: 0, mt }`）。端末は各サルを今いる位置から歩道のグラフに乗せ直して続ける |

**ルームが引き継ぐ前の確認**: ホストのスナップショットの群れが正しい形（番号・座標・HP の範囲）でなければ引き継がない（1 回の壊れたデータで群れが全滅したままになるのを防ぐ）。

**ルームが判定すること**: サルへの弾（撃った人が生きていて表世界、連射間隔、射程、胸の高さ 1.05 m への視線。サルは弾が届く間に動くので、サルから 2.5 m 以内の壁は遮りとみなさない）。
倒した人は**ルームが全員に伝える**（`mdeath`。ルームが動かしている間はホストの `mdeath` は通さない）。プレイヤーが投げた死体（車にはねられた・撃たれた通行人）もルームの群れに入り、食べに来る。
サルの引っかきは今までどおり各端末が自分で判定する（→ B-9 ②+ でルームの判定に変更）。

**眠りから覚めたルーム**（全員 10 秒以上無操作の後）: 端末は「ルームが動かしている」と思ったまま、ルームは何も動かしていない、という食い違いがあった（B-9 ① からの潜在的な不具合）。
覚めて最初のメッセージで、ルームは全員に「何も動かしていない」（`own { b: 0, m: 0 }`）と伝え、端末は自分で続け、2人以上ならルームが改めて引き継ぐ。

**通信**: 群れは今までホストが 1 秒に 8 回ほかの人へ送っていたもの（同じ形式）を、ルームが 1 秒に 10 回、全員へ送る。ホストの送信からは群れが消える。

**確認**:
- `server/test/troop.test.js` 24/24 — 歩道を歩き建物に入らない、死体を食べて片付ける、撃たれると群れ全体が一度だけ逆上（撃ち直しで時計は戻らない）、控えが現れて近いプレイヤーへ走る、
  倒れた猿は死体が消えてから起き上がる、1 分で静まる、スナップショットから引き継いで飛ばない／ルームが引き継ぐ、ホストの群れを通さない、全員へ送る、
  弾が当たる（20）、連射・裏世界・建物越しは当たらない、倒した人をルームが伝え、ホストは言えない、死体がルームの群れに入る、逆上中 1 分で 1 回の処理は最大 4 ms 程度、
  1 人に戻ると返す、壊れた群れは引き継がない、眠りから覚めたら「何も動かしていない」と伝え、ホストが群れを送らなければ自分で始める。
  **処理の重さ**: 逆上した群れ（21 匹）と 2 人で 1 分、1 回の処理は平均 0.03 ms・最大 1 ms 以下
- `lab/test/troop.game.test.js` 9/9（本物のゲーム）— 1 人のうちは端末が動かす／2 人目でルームが**誤差 0.2 m** で引き継ぎ、ホストは送らなくなる／
  両方の画面でサルの位置の差は最大 0.3 m／撃つとルームの判定で当たり（60 → 40）、両方の画面で群れが逆上／倒すと両方の画面で消え、撃った人の裂け目の数に入る／
  1 人に戻ると群れが返され、逆上したまま続く（その瞬間の移動は最大 4.5 m、走っている分）
- 既存のゲームテスト（群れを端末で動かすもの・ルームで動かすもの）: `phase3` 10/10、`solo` 4/4、`snapshot` 6/6、`gatesync` 18/18、`server` 20/20、`boss` 11/11、`mind` 11/11 — 切り出し前と同じく通る

### B-9 ③ 表世界の交通（車・スポーツカー・歩行者）もサーバーへ（2026-09-24、ユーザーの選択「A」）

**やる前の測定**（`updateCity` 1 フレーム、この機械）: 交通の処理のうち **計算が 2/3、描画の準備が 1/3**（563 µs のうち計算 376 µs）。
端末が計算をやめてサーバーの位置を使えば、その分が軽くなる。代わりに通信が増える。**2 人以上のときだけ**ルームが動かす（1 人では今までどおり端末、通信ゼロ）。

| 段 | 内容 |
|---|---|
| 1 | 交通の計算を **`shared/traffic.js`** に切り出し（車 140・スポーツカー 6・歩行者 215。車線ごとの前車との距離、信号、交差点、ドリフト、はね飛ばし）。**1 つの種（seed）から作る**ので、どの端末とルームでも同じ街で始まる（どの車がタクシーで何色か、歩行者の服も同じ） |
| 2 | ルームが動かす交通 **`server/traffic.js`**（20 Hz）。信号は街で 1 つの時計（ルームの時計）で、端末の信号表示もその時計に合わせる。サルもこの時計で横断する |
| 3 | **送り方**（推測航法）: 端末は、最後に言われた位置と速度でそれぞれを動かし続ける。ルームはプレイヤーごとに「その端末が今どこに描いているはずか」を覚えていて、**本当の位置からずれた時だけ**送り直す。ずれの許容は距離で変える（120 m 以内 0.3 m、260 m 以内 1 m、その先は霧で 3 m）。次の送信（0.1 秒後）までにずれそうな時（ブレーキ中など）は先回りして送る。まっすぐ走る車はほぼ何も送らない。裏世界にいるプレイヤーには何も送らず、戻った時に全部送る |
| 4 | **受け渡し**: 2 人目が来ると、ルームはホストの端末に交通を丸ごと求め（`tq` → `tfull`。各車の位置・向き・速度と**道のグラフ上の正確な位置**、約 8.7 KB）、そこから続ける。ホストの画面では何も跳ばない。答えがなければ種から始める。1 人に戻ると最後の状態を添えて返す（`own { t: 0, tf }`） |
| 5 | **歩行者を撃った時**: ルームが判定し（撃った人が生きていて表世界、連射間隔、射程、胸 1.15 m への視線）、その歩行者を別の場所へ戻す。死体は今までどおり全員の画面に出る。スポーツカーがはねた歩行者の死体はルームが全員に出し、サルの群れにも入る |

**結果**（`server/test/traffic.test.js` 21/21、`lab/test/traffic.game.test.js` 9/9、`server/test/room.test.js` にルーム間の受け渡しを追加 27/27）:

| 項目 | 値 |
|---|---|
| 端末の交通の処理（`updateCity`、この機械） | 自分で計算 **488〜514 µs** → ルームに言われる **372〜377 µs**（**−24〜27%**）。残りは描画（161 µs）と、ルームの位置へのなめらかな追従・車輪や手足の計算（151 µs）。携帯では 1 フレーム 0.5〜0.9 ms 軽くなる見込み（実機では未計測） |
| 受信量（1 人あたり、街全体） | **2.6〜3.2 KB/s**（1 時間で約 10 MB） |
| 端末の表示と本当の位置のずれ（99% の時間） | 120 m 以内 **0.26 m**、260 m 以内 0.90 m、その先 2.8 m。最大は角を曲がる瞬間の 1.2 m（0.1 秒で直る）。スポーツカー（25〜46 m/s）は最大 2.3 m（その 0.1 秒分） |
| 2 人の画面の差 | 車 0.44 m 以内、歩行者 0.59 m 以内。時計（信号）は 2 人とルームで 0.01 秒以内 |
| 受け渡し | ホストの画面で跳ぶ車 **0 m**。返した後も続けて走る（その 1 秒で最大 7.6 m ＝走った分） |
| ルームの処理 | 2 人で 1 分、1 回の処理は平均 0.36 ms・最大 3.9 ms（無料プランの上限 10 ms） |

**途中で見つけて直したこと**:
- 1 人に戻る時の返却メッセージで、サルの群れの状態と交通の「動かしている」フラグが同じ名前（`t`）になり、端末が「まだルームが交通を動かしている」と読んで止まったままになった → 群れの状態を `mt` に変更
- 受け渡し直後に車線の一覧が空で、最初の 1 ステップだけ前の車が見えなかった → 受け渡し時に一覧を作り直す
- 作ったばかりの街は描画位置がまだ無く、そのまま渡すと不正なデータになった → 作った時点で位置を持たせる
- 眠りから覚めたルームは、ホストへの「交通を送って」を、まだ誰にも送れない起動中に出していた → 覚めて最初のメッセージで改めて頼む
- （BEELZEBUB）プレイヤーについての記憶が戦いの始まった後に届くと（その人の端末が送ったもの・サーバーの記憶の返事）、その戦いでは使われなかった → 届いた時点で使う
- （テスト）`boss.game.test.js` の射撃は、狙ってすぐ撃っていた。`fire()` はカメラの向きで撃ち、カメラは次のフレームで向く。
  彼が動くと外れることがあり、交通の分だけ処理が増えて外れやすくなった → 狙ってから 3 フレーム待って撃つ（`troop.game` と同じ）

**そのまま残したこと**: 車にはねられた判定は各端末が自分に対して行う（位置は全員同じになったので、見えている車に当たる）。
ルームが眠ると交通の状態は失われ、起きた時に端末から受け取り直す（B-9 ② の「何も動かしていない」通知で）。

### B-9 ②+ 実機の報告から: 小さいサル・サルの判定を全部ルームへ・遠くのプレイヤーの HP バー（2026-09-24）

ユーザーの報告（iPhone・横持ち・2 人、F3 に `TROOP room`）: 「猿は本当にサーバーで完全処理できているのか」「照準の近くに小さい猿がいた」「横画面で、離れた他のプレイヤーの HP バーが異様に大きい」。

**小さいサルの原因**: 倒れたサルは端末で縮みながら消える（死体の演出）。ルームが群れを動かしている時、そのサルがルームで起き上がると、
端末は HP と位置だけを受け取り、**縮んだ大きさ・倒れた向き・死体の状態を戻していなかった**。起き上がった猿が縮んだまま歩いていた。
（1 人のときは端末が自分で起こすので、その処理で元に戻っていた。）
→ ルームのスナップショットで「倒れていた猿が起きた」と分かった時点で、大きさ（猿の本来の大きさ 0.9、`MONKEY_SCALE`）・向き・位置・死体の状態を戻す。
消えていた猿（控え）が現れる時もシーンに戻す。

**サルの判定で、まだ端末に残っていたもの**（B-9 ② で「今までどおり各端末」としていたもの）をルームへ:

| | 前 | 今（2 人以上） |
|---|---|---|
| サルの引っかき | 各端末が、自分とサルの距離で判定して自分の HP を減らす | **ルーム**が判定（逆上したサルから 1.9 m 以内、11 ダメージ、同じサルは 0.7 秒に 1 回、1 人が受けるのは 0.4 秒に 1 回まで ＝前と同じ値）。HP はルームが持ち、端末には `hp { src: 'monkey' }` で伝わり、画面の揺れと音が出る |
| 倒したサルの数（裂け目の数） | 撃った端末が、自分の画面で倒れたら数える | **ルームが「倒した人」を全員に伝えた時**（`mdeath { by }`）に、その人の端末が数える。自分の画面で倒れて見えただけでは数えない |

これで 2 人以上のとき、サルについて端末がすることは**描くことだけ**（動き・逆上・起き上がり・弾の当たり・倒した人・引っかき・死体を食べる、はすべてルーム）。
1 人のときは今までどおり端末が全部行う（通信なし）。

**他のプレイヤーの HP バーと名前**: 近くでは画面上で一定の大きさ（バー 46 px）にしていたため、**遠くの小さく見える相手にも同じ 46 px のバー**が付いた。
横持ちは画面の高さが低く、人が小さく映るので特に目立った。→ 相手が画面上で 36 px 以上の背丈に見える間は今の大きさ、それより小さくなると相手に合わせて縮む（最小 55%）。
スコープの拡大と画面の向きも計算に入る。

**BEELZEBUB のバー**: 名前の一覧（ロスター）と重なっていた → 一覧の下に置く（一覧の行数と自分の HP バーの位置から毎回計算）。横持ちでは幅を 320 px までに。
`lab/tools/hud_layout.js` に BEELZEBUB のバーを加え、縦・横とも重なり 0。

**確認**:
- `server/test/troop.test.js` 27/27 — 引っかき 3 項目を追加（逆上したサルの届く所に立つとルームが 11 減らし「引っかき」と伝える、0.7 秒に 1 回より速くない、裂け目の向こうの人は引っかかれない）
- `lab/test/troop.game.test.js` 11/11 — 逆上中に倒したサルがルームで起き上がると、**両方の画面で本来の大きさ（0.9）**に戻る／逆上したサルの届く所にいると、ルームの判定で HP が減り、端末で揺れと音が出る
- `server/test/*` すべて、`room.test.js`（workerd）27/27
- 既存のゲームテスト: `traffic` 9/9、`boss` 11/11、`mind` 11/11、`solo` 4/4、`snapshot` 6/6、`gatesync` 18/18、`phase3` 10/10、`phase7` 16/16、`server` 20/20、`move` 9/9、`collision` 1/1、`phase11` 4/4
- 本番（公開後の確認）28/28
- （テスト）`boss.game.test.js`: 2 人目が来てから撃つまで彼の近くに立っている間に、彼に斬られて倒れていることがあり、その時は撃てなかった（4 回に 1 回ほど）。
  ゲームの不具合ではなく、テストの手順 → 撃つ前に 2 人とも起き上がらせる。4 回続けて 11/11

### B-9 ④ 裏世界の犬（デモドッグ）もサーバーへ（2026-09-25、ユーザーの指示「犬いけ」）

**やる前に分かったこと**: 犬は今までホストの端末が動かし、**ホスト本人しか見ていなかった**（見る・噛む・追う相手は、その端末のプレイヤーだけ）。
ほかのプレイヤーは犬に見つからず、噛まれもしなかった。ルームが動かすと、裏世界にいる全員が相手になる。

犬は、ホストの端末に残るボスたち（マインドフレイヤー・VECNA・デモゴルゴン）と深くつながっている
（従える・呼び出す・命令する・連れて瞬間移動する・傷を分け合う・押しのける・廃車を投げる）。この順番で移すため、
**ボスたちはまだホストの端末で動き、犬への働きかけをルームに中継する**形にした（ボスをルームに移す時に、この中継はルームの中の処理に変わる）。

| 段 | 内容 |
|---|---|
| 1 | 建物の中を歩く処理（床の高さ・壁から押し出す・どの建物か・ドアと階段の道順）を **`shared/city.js` の `ground()`** に。ゲームはプレイヤーもすべての生き物もこれで歩く。ルームは建物の中身を、初めて必要になった時に作る（全部作ると 85 ms かかり、1 回の処理の上限 10 ms を超えるため） |
| 2 | 裏世界の廃車（置き場所・体を押し出す・弾を止める）を **`shared/wrecks.js`** に。種と車の形から、ゲームとルームで同じ 96 台 |
| 3 | 犬の頭脳（聞く・見る・決める・動く・噛む・引く・隠れる・仲間を呼ぶ・群れ・地区への補充・主人への従い方・デモゴルゴンに押しのけられる）を **`shared/dogs.js`** に。狙う相手は「プレイヤーの一覧」から選ぶ（ゲームは自分 1 人、ルームは裏世界の全員） |
| 4 | ルームが動かす犬 **`server/dogs.js`**（20 Hz）。裏世界にいる人の位置・向き・倒れているか・透明か・走る足音・銃声を使う。噛むのもルームが判定（2.25 m 以内・同じ階・9 ダメージ・同じ人は 0.42 秒に 1 回まで） |
| 5 | 受け渡し（`dq` → `dfull`、`own { d }`）。2 人目が来るとホストの犬を丸ごと受け取って続け（位置・状態・狙い・主人・クールダウンまで）、1 人に戻ると最後の状態を添えて返す |
| 6 | **ボスたちとの中継**: ルームはホストのスナップショットからボスの位置を読み（従う犬の護衛・押しのけ・VECNA の周りを空ける）、ホストの端末はボスが犬にしたこと（従えた・狙いを渡した・状態を変えた・連れて移動した・呼び出した・廃車を持ち上げた／投げて落ちた場所）を毎フレームの差分として送る（`dord`）。逆にルームは、犬が知っていること（`dk`、ボスは犬を通して学ぶ）と、フレイヤーのつながりの中で犬が傷ついたこと（`dshare`、フレイヤーも血を流す）をホストに伝える。呼び出しは主人ごとの上限（フレイヤー 3・VECNA 6）と地区の上限 48 をルームが守る |

**1 人プレイの犬が 1 歩も変わらないことの確認**: `lab/tools/dog_trace.js` — 乱数を固定し、時計を手で進めて、犬を 1350 ステップ動かし（護衛・追跡・噛みつき・仲間呼び・傷・死を含む）、
全員の状態の指紋を取る。移す前と、段 1・2・3 のあと、すべて同じ（`bcd54b4e`、67 KB の記録がバイト単位で一致）。

**送り方**: 裏世界にいる人にだけ、1 秒に 10 回（`dv`、今までホストが送っていたのと同じ形）。8 回に 1 回は全部、その間は誰かの 110 m 以内の犬と、体力・状態・印が変わった犬だけ。昼側の人には送らない。

**ホストの端末の負担**: ルームの犬に追従するだけになったので、遠くて描かない犬（90 m より先）は 4〜12 フレームに 1 回だけ位置を合わせる
（全 48 匹を毎フレーム追従させると、自分で動かすより重かった）。

**途中で見つけて直したこと**:
- （ゲームの不具合、前からあったもの）噛みついた直後、飛びかかりの途中で撃たれた犬は「噛んだ」印が残ったままになり、**その犬は二度と噛まなくなっていた** → 飛びかかりの始めに印を戻す（1 人プレイの記録は変わらない：記録の中ではこの場面が起きていない）
- ルームで、建物の中身を初めて作る処理が 1 回に重なると 1 回の処理が 9.5 ms を超えることがあった → メッセージのたびに 1 棟ずつ先に作っておく（約 11 秒で全 213 棟）。以後は最大 1〜3 ms
- 裏世界に誰もいなくなって犬を消した後、次に誰かが渡った時、前の補充の待ち時間（最大 7.5 秒）が残っていた → ルームでは消した時に待ち時間を戻す
- 召喚された犬が地面から這い出てくる様子は、ほかの人の画面では出ていなかった → スナップショットの印に「這い出し中」を加えた（ホストが動かす場合も含めて、全員に見える）
- （テスト）`traffic.game.test.js` が今日の混んだ機械で揺れた: 受け渡し直後の「跳び」を 0.6 秒として測っていたが、両方の画面の確認を待つ分だけ実際は 0.7〜1.6 秒経っていた →
  経過時間を実際に測る（跳びは毎回 0 m）。端末の処理時間は、1 回目と 2 回目で立ち位置が違い（周りの車の数が違う）、平均が GC 等の一瞬の重さに引っ張られていた →
  同じ交差点で測り、上下 1 割を除いた平均で比べる（24〜34% 減）。`troop.test.js` の引っかきの確認は、直前に別の猿が引っかいた直後だと 0.4 秒の間隔で数えられなかった（CI で 1 回）→ ほかの猿を遠ざけてから確かめる
- （公開後の確認）サーバーのコードが変わらない push（文書だけ等）では、ビルドの指紋が前と同じなので、部屋が新しい版へ移っている最中でも確認を始めてしまい、
  途中で部屋が移されて 15/29 が失敗した（再実行で 29/29）→ Worker に版の ID（Cloudflare の version metadata）を持たせ、部屋の最初の返事に載せ（`ver`）、
  確認は公開した版の ID の部屋を待つ。あわせて、Markdown だけの push では公開しない（何も変わらないのに、遊んでいる人の部屋が一瞬つなぎ直しになるため）

**確認**:
- `server/test/dogs.test.js` 32/32 — 補充（地区全体、プレイヤーの 34 m 以内には出ない）、壁の中や地図の外に行かない、銃声で様子を見に来る、近くの犬が見つけて噛む、傷つくと引く、死んで片付く、状態の受け渡し／
  ルーム: ホストに犬を求めて続ける、裏世界の人にだけ送る、ホストの犬は通さない、弾（当たる 92→72・連射・昼側・建物越しは当たらない）、噛む（9 ダメージ、昼側の人は噛まれない）、
  ボスの位置を読む、従えられた犬は護衛する、狙いを渡される、召喚は上限 3 まで・這い出す、犬の知識をホストへ、傷の共有、デモゴルゴンに押しのけられる、壁の中への移動は断る、
  誰もいなくなると空に・渡ると補充、1 分間 2 人を追う（1 回の処理 平均 0.35〜0.47 ms・最大 1〜7 ms）、送信量 1 人あたり 2.7 KB/s、1 人に戻ると返す、眠りから覚めると「動かしていない」と伝えて求め直す
- `lab/test/dogs.game.test.js` 10/10（本物のゲーム 2 人）— 1 人のうちは端末／2 人目でルームが引き継ぎ、ホストの画面で跳ぶ犬 **0 m**／裏世界で 2 人とも同じ犬（ずれ 0.00 m）／
  **2 人目も噛まれる**（今までは噛まれなかった）／2 人目が撃った犬の傷がルームの判定で両方の画面に／ホストのフレイヤーが護衛を呼ぶと、ルームが 3 匹作り、全員に見える／
  受信 **2.1〜2.4 KB/s**／1 人に戻ると犬が返され、続けて動く／**ホストの端末の犬の処理 44〜75% 減**（同じ場面で、自分で動かす 509〜1120 µs → ルームに言われる 214〜303 µs）
- `lab/test/city.test.js` 7/7 — 6000 か所でゲームとルームの床・壁・建物・道順が一致、廃車 96 台が一致
- `server/test/room.test.js` に犬の受け渡しを追加: workerd 28/28、**本番 29/29**（Cloudflare 上で 1.8 秒で 36 匹を補充、裏世界にいる人にだけ送る）
- 既存のゲームテスト: `solo` 4/4、`snapshot` 6/6、`phase3` 10/10、`gatesync` 18/18、`troop` 11/11、`traffic` 9/9、`boss` 11/11、`mind` 11/11、`phase7` 16/16、
  `server` 20/20、`move` 9/9、`collision` 1/1、`phase11` 4/4。サーバーのテスト一式すべて

**そのまま残したこと**: デモゴルゴン・マインドフレイヤー・VECNA はホストの端末で動く（次の段階で移す）。その間、ボスがルームの犬を見る情報は 0.5 秒ごと（`dk`）、
ボスの位置は 1 秒に 8 回（ホストのスナップショット）なので、ホストだけで動いていた時より少し遅れる。

### B-9 ⑤ デモゴルゴンもサーバーへ（2026-09-25、ユーザーの指示「デモゴルゴンもやってくれ」）

犬と同じ手順。デモゴルゴンも、犬と同じく**ホスト本人しか狙っていなかった**（ほかのプレイヤーは見つからず、攻撃も当たらなかった）。

| 段 | 内容 |
|---|---|
| 1 | 頭脳（見る・聞く・追う・回り込む・4 種の攻撃の予備動作と当たり判定の窓・怒り・護衛・地区への補充・受けた傷の反応）を **`shared/gorgons.js`** に。狙う相手は犬と同じ「プレイヤーの一覧」から選び、回り込む先は**その相手が見ていない方向**（相手の向きを使う） |
| 2 | ルームは犬と一緒に動かす（`server/dogs.js`）。同じプレイヤーの一覧・同じ物音・同じ廃車・同じボスの位置を使い、犬はルームのデモゴルゴンを避けて押しのけられる。攻撃が当たるかはルームが判定（届く距離・同じ階・同じ人は 0.5 秒に 1 回まで、怒り中は 1.25 倍）。「顔」の攻撃（捕食）は `gorpred` として伝わり、端末で強い揺れと一瞬の拘束 |
| 3 | 受け渡しは犬と一緒（`dfull` に `g`、返す時は `df.g`）。怒っているか・どれだけ傷ついているか・主人・狙いも引き継ぐ |
| 4 | ボスたち（フレイヤー・VECNA）の働きかけは、犬と同じ差分の中継に `g` の付いた命令として乗る（従える・狙いを渡す・状態を変える・連れて移動・呼び出し）。呼び出しの上限はフレイヤー 2・VECNA 2。デモゴルゴンが知っていることも `dk` でホストへ、フレイヤーのつながりの中で傷ついたら `dshare` |
| 5 | 裏世界にいる人に、犬と一緒に 1 秒に 10 回（`dv` の `q`、最大 8 体なので毎回全部） |

**1 人プレイのデモゴルゴンが 1 歩も変わらないことの確認**: `lab/tools/gor_trace.js` — 乱数を固定し時計を手で進め、2400 ステップ
（25 m から追わせる・遠くから撃たれて回り込む・振りかぶり中の重い一撃・怒り・フレイヤーと VECNA の護衛・銃声・死）の指紋が、移す前と同じ（`8c1365c8`、38 KB がバイト単位で一致）。犬の記録も同じまま（`bcd54b4e`）。

**途中で見つけて直したこと**:
- （前回の犬の中継の穴）フレイヤーや VECNA が**撃たれた時**の働きかけ（撃った人の方へ護衛を向かわせる、VECNA が撃たれて瞬間移動する時に取り巻きを連れていく、VECNA が倒れて犬が逃げる）は、
  毎フレームの差分の外（弾が当たった瞬間、ルームからの知らせを受けた瞬間）で起きるので、ルームに届いていなかった → その処理の前後でも差分を取って送る（`withOrders`）
- （前からあった不具合）ほかの人の画面でデモゴルゴンがスナップショットから作られる時、送られてきた番号ではなく最初の空き番号に入っていた（番号がずれると、その体が消えては作られ直す）→ 番号どおりに作る
- ルームに受け渡す時、ホストの端末は「デモゴルゴンを表示中か」の印で送るかを決めていたため、裏世界に入った直後（まだ 1 フレームも回っていない時）は 0 体として渡すことがあった → いる分を常に送る

**確認**:
- `server/test/gorgons.test.js` 22/22 — 補充（6 体、互いに離れ、プレイヤーの 58 m 以内には出ない）、見つけて近づき振りかぶって当てる、遠くから撃たれると回り込む、3 割以下で怒る、死んで片付く、状態の受け渡し／
  ルーム: ホストのデモゴルゴンを傷ごと引き継ぐ、裏世界の人にだけ送る、ホストのは通さない、弾（420→400・連射・昼側は当たらない）、攻撃（ルームが判定して「デモゴルゴン」と伝える・昼側の人は当たらない）、
  フレイヤーに従えられて護衛する、狙いを渡される、呼び出しは上限 2 まで・這い出す、知識をホストへ、傷の共有、VECNA の周りは空ける、
  犬と一緒に 1 分間 2 人を追う（1 回の処理 平均 0.55 ms・最大 5.2 ms）、1 人に戻ると犬と一緒に返す
- `lab/test/gorgons.game.test.js` 9/9（本物のゲーム 2 人）— 1 人のうちは端末／2 人目でルームが**傷ついたまま**引き継ぎ、ホストの画面で跳ぶもの 0 m／2 人とも同じデモゴルゴン（ずれ 0.00 m）／
  **2 人目も襲われる**／2 人目が撃った傷が両方の画面に／ホストのフレイヤーがデモゴルゴンを呼ぶと、ルームが 2 体作り全員に見える／
  **フレイヤーがフレームの外で撃たれても、取り巻きへの命令がルームに届く**／1 人に戻ると返され、続けて動く
- `server/test/dogs.test.js` 32/32（犬が避けて押しのけられる相手は、ルーム自身のデモゴルゴンに）

**そのまま残したこと**: マインドフレイヤーと VECNA はホストの端末で動く（次の段階）。

### Phase 17 実機テスト — 1回目（2026-09-24、iPhone 14・Safari・4G、本番 URL に `?debug`、1人）

| | 縦持ち | 横持ち |
|---|---|---|
| FPS | **60**（1フレーム 16.7 ms、最悪 17.0） | **60**（16.7 ms、最悪 26.0） |
| JS（ロジック / 描画） | 1.4 / 1.8 ms（最悪 3.0 / 3.0） | 0.9 / 1.4 ms（最悪 4.0 / 6.0） |
| ドローコール / 三角形 | 55 / 448k | 111 / 618k |
| 画質 | 3/3・×1.0（自動のまま下がらない。ピクセル比 2） | 同じ |
| 通信 | 往復 **63 ms**、送信 12.7 KB/s・25 通/s、受信ほぼ 0 | 往復 65 ms、送信 14.6 KB/s・24 通/s |
| 場面 | 裏世界、犬 42・デモゴルゴン 2 | 裏世界、犬 47・デモゴルゴン 6 |

**わかったこと**:
- **性能は十分**: 最高画質のまま 60 FPS（この機種の画面の上限）。JS は 1 フレーム 16.7 ms のうち約 3 ms で、8 割ほど余裕がある。
  A で保留した「もっと細かいカリング」と「インスタンスの部分転送」は、この機種では不要（GPU の余裕も、画質が自動で下がらないことで確認）
- **ヒープ**は Safari では取れない（`n/a`）。メモリの増加は A4 の長時間テスト（`soak.js`）で見ている
- **日本の 4G からルームまで往復 63 ms**（ルームはアジア太平洋に置いている。GitHub のアメリカのサーバーからは 228 ms）
- **問題1 — 1人でも毎秒 25 通・12.7 KB を送っていた**（位置 18 回＋敵のスナップショット 8 回。見る人はいない）。
  スマホのデータ通信で **1時間に約 46 MB**、ルームの無料枠（受信 20 通で 1 リクエスト、1日 10 万）では **1人で遊ぶだけで 1日約 22 時間分**を使い切る量。
  → **直した**: 部屋に自分しかいないときは位置を 1 秒に 1 回、敵のスナップショットは送らない。誰かが入った瞬間に元の頻度に戻す
  （新しい人の位置が届いた時点で、自分の位置と敵を即座に送る）。アイテムを取るときは、その直前に位置を送る（部屋が「そこにいる」ことを確かめられるように）。
  1人のときの送信は **約 1.3 通/s・0.1 KB/s（約 0.4 MB/時）**（ハーネスで実測）。無料枠では 1人なら 1日 400 時間分以上になる。
  `lab/test/server.game.test.js` に確認を追加（1人で 1 通/s、2人で 24 通/s に戻る、1人でもバナナが取れる）
- **問題2 — 画面上の重なり**（元からあったもの）: 縦持ちで操作説明と名前の一覧、K/D と望遠鏡ボタン、3人称ボタンとスティック。
  横持ちで FPS 表示と JUMP、チャットボタンとミニマップ・HP。両方で ⟳（リロード）と JUMP が 25 pt 重なり、押し間違いが起きうる。
  → **直した**: `lab/tools/hud_layout.js`（iPhone 14 の大きさとセーフエリアで配置し、文字は字形、丸いボタンは円として重なりを測る）で
  **19 組 → 0 組**。縦持ちは弾数・K/D・説明を HP の下の空いた左側へ、横持ちは HP をミニマップの横へ、説明と FPS を歯車の横へ。
  ⟳ は JUMP と「透明」のあいだへ。ボタン配置の編集で動かしたボタンは、その位置のまま
- 2人以上のとき（1人あたり毎秒 25 通）の無料枠: 2人で 1日約 11 時間、4人で約 5.5 時間。送信の大半は敵のスナップショット（約 1.3 KB × 8 回/秒）で、
  B-14（通信の最適化: 差分・コンパクト形式）で減らせる
- テストについて: `server/test/room.test.js` が CI で一度だけ落ちた（混んだマシンで、決めうちの 300 ms 待ちが死亡前の状態を読んだ。
  サーバーのコードは変えていない回）。届くはずのものは届くまで待つ（最大 3 秒）ように直した。
  `lab/test/phase3.game.test.js` の射線の一致（4 回に 1 回ほど 252〜274/300）と `phase11.game.test.js` の時間の上限は、
  変更前のゲームでも同じように落ちる（前者はスポーン位置、後者はマシンの混み具合による）。前者は別の作業として切り出した
